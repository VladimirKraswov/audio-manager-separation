# Source this file from /home/vladimir/audio_manager_separation after installing
# the corresponding model runtimes:
#
#   source env.tse
#
# These commands are consumed by process_call.py through --models.

export WESEP_TSE_CMD='python scripts/run_wesep_tse.py --mixture {mixture} --reference {reference} --output {output} --sample-rate {sample_rate} --device {device}'
