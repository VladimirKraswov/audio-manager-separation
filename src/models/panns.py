from __future__ import annotations


def analyze_panns(*_args, **_kwargs) -> dict:
    return {
        "available": False,
        "note": "PANNs adapter placeholder. Add a torch PANNs checkpoint for production tagging.",
    }
